interface:
  display_name: "UI Grammar"
  short_description: "Describe React UI systems and routes precisely"
  default_prompt: "Use $ui-grammar to describe this UI as a source-grounded component system. Keep the result disposable unless durable law warrants persistence."
